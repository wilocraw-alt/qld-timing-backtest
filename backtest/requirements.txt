# Python 3.8 호환 핀 (이 환경은 python3.8.10).
# multitasking 최신판은 type[Thread] 문법(3.9+)을 써서 py3.8에서 yfinance import가 깨짐 → 0.0.11로 핀(필수).
# yfinance는 py3.8에서 검증된 0.2.x 사용.
pandas>=2.0,<2.1
numpy>=1.20,<1.25
yfinance>=0.2.50,<1.0.0
multitasking==0.0.11
pyarrow>=10.0.0
matplotlib>=3.5
pyyaml>=6.0
