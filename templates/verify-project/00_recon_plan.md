# T0 정찰·검증계획 — <대상명>

- 대상 경로: `<path>`
- 자체 git: <yes/no> (있으면 identity·미수정 원칙)
- 생성일: <YYYY-MM-DD>

## 표면 분류
- 감지 표면: `<web|api|cli|lib|tui>` (복수 가능)
- 근거: <라우트/UI/엔트리포인트/패키지 신호 1~2줄>
- 적용 드라이버: `surfaces/<표면>.md`

## 구조·진입점
- 주요 컴포넌트: <모듈/서비스 목록>
- 진입점/실행법: <명령·포트·compose>
- 외부 의존: <DB·LLM·MCP·큐 등 + 연결정보 출처(.env 키 이름만)>

## 정상 동작 케이스(후보 — T3 입력)
1. <케이스명> — <입력 → 기대>
2. ...

## 단계 계획
- T1 코드검증: <어떤 컴포넌트를 어떻게>
- T2 설치검증: <기동 방법·헬스 기준>
- T3 기능검증: <케이스 × 표면 드라이버>
- T4 적대적: <카탈로그에서 선별한 BD/ML/TY/HV>
- 산출 위치: `<out>`
- 수행 범위: `--stages <range>`
