# T2 설치검증 보고서 — <대상명>

- 환경: <OS/런타임 버전, venv/node/docker>
- 환경파일: 대상 `.env` 사용(미수정·비밀 미출력) | `.env.example` 기반

## 설치 절차(재현)
1. <의존성 설치 명령>
2. <빌드 명령>
3. <외부 의존 준비(DB/LLM/MCP) — 연결출처는 .env 키 이름만>

## 가동 상태
- <컴포넌트> | 포트 <p> | PID <pid> | 로그 `<path>` | 헬스 <결과>
- ...
- 매니저 독립확인: <curl/health/docker ps 결과 요약>

## 환경 갭(있으면)
- <항목> — <플랫폼 버그 아님 근거 (미설치 SDK·미가동 레거시 등)>

## 재기동 명령
```bash
<죽었을 때 되살리는 절차>
```

## 판정
- 기동: <성공 | 부분 | 불가(사유)>
